F1Добро пожаловать в Ubuntu!Это live CD-ROM для Ubuntu 6.06,
выпущенный 20060806.1.

ИНДЕКС ПОМОЩИ

КЛЮЧ   ТЕМА

<F1F1>   Эта страница индекс помощи.
<F2F2>   Требования для запуска Ubuntu.
<F3F3>   Способы загрузки для специальных
вариантов использования этого CD-ROM.
<F4F4>   Дополнительные способы загрузки; режим
восстановления.
<F5F5>   Специальные параметры загрузки, обзор.
<F6F6>   Специальные параметры загрузки для
специальных платформ.
<F7F7>   Специальные параметры загрузки для
выбранных дисковых контроллеров.
<F8F8>   Специальные параметры загрузки для
системы bootstrap.
<F9F9>   Как получить помощь.
<F10F10>  Авторские права и гарантии

Нажмите клавиши от F2 до F10 для деталей, или Escape для выхода из помощи.F2ТРЕБОВАНИЯ ДЛЯ УСТАНОВКИ UBUNTUУ вас должно быть хотя бы 128 мегабайт оперативной памяти для использования
live системы Ubuntu.

Live система не требует пространства на вашем жестком диске. Однако будут
использованы существующие доступные разделы подкачки.

Прочтите FAQ для для получения дополнительной информации; этот документ
доступен на сайте Ubuntu, http://www.ubuntu.com/

Спасибо что вы выбрали Ubuntu!

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F3СПОСОБЫ ЗАГРУЗКИДоступные способы загрузки:

live
  Запустить live систему.
memtest
  Выполнить тест оперативной памяти.

Для использования одного из методов загрузки, введите его в консоли, если
необходимо с параметрами загрузки. Например:

  boot: live acpi=off

Если вы неуверены, используйте стандартный метод загрузки без специальных
параметров, просто нажав Enter.

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F4РЕЖИМ ВОССТАНОВЛЕНИЯВоспользуйтесь одним из этих методов для
восстановления существующей установки

rescue
  Загрузиться в режиме восстановления.

Для использования одного из методов загрузки, введите его в консоли, если
необходимо с параметрами загрузки. Например:

  boot: rescue acpi=off

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F5СПЕЦИАЛЬНЫЕ ПАРАМЕТРЫ ЗАГРУЗКИ - ОБЗОРНа некоторых системах Вам может понадобиться указать параметр в boot:
консоли для загрузки системы. К примеру, Linux может автоматически не
определить ваши устройства, и вам придется явно указать их местонахождение
или тип для правильного определения.

Для дополнительной информации о параметрах загрузки, которые вы можете
использовать нажмите:

<F6F6>   загрузочные параметры для специальных
платформ
<F7F7>   загрузочные параметры для различных
дисковых контроллеров
<F8F8>   загрузочные параметры определяемые
системой bootstrap

Многие модули ядра динамически загружаются установщиком и параметры для этих
модулей не могут быть введены через командную строку. Для получения
возможности ввода параметров во время загрузки модулей загрузитесь в режиме
эксперта (см. <F3F3>).

Многие модули ядра динамически загружаются системой bootstrap и параметры
для этих модулей не могут быть введены через командную строку.

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F6СПЕЦИАЛЬНЫЕ ПАРАМЕТРЫ ЗАГРУЗКИ -
РАЗЛИЧНЫЕ УСТРОЙСТВАВы можете использовать следующие параметры загрузки в boot: консоли в
сочетании с методами загрузки (см. <F3F3>>). Если вы используете
шеснадцатиричные числа вы должны использовать префикс 0x (например, 0x300).

УСТРОЙСТВА                             ВВОДИМЫЙ ПАРАМЕТР

IBM PS/1 или ValuePoint (IDE диск)     hd=cylinders,heads,sectors
Некоторые IBM ThinkPads                floppy=thinkpad
IBM Pentium Microchannel               mca-pentium no-hlt
Protect I/O port regions               reserve=iobase,extent[,...]
Workaround faulty FPU (старые машины)  no387
Ноутбуки с проблемами отображения экрана
                                       vga=771
Если ваша система зависает при загрузка и последнее сообщение которое вы
видите "aec671x-detect..", попробуйте  gdth=disable:y
Если у вас происходят зависания или другие аппаратные ошибки, отключите
ошибочный роутинг прерывания APIC      noapic nolapic

Например:

  boot: live vga=771 noapic nolapic

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F7СПЕЦИАЛЬНЫЕ ПАРАМЕТРЫ ЗАГРУЗКИ -
РАЗЛИЧНЫЕ ДИСКИВы можете использовать следующие параметры загрузки в boot: строка
приглашения, в сочетании с методом загрузки (см. <F3F3>).

УСТРОЙСТВА                   ВВОДИМЫЙ ПАРАМЕТР

Adaptec 151x, 152x           aha152x=iobase[,irq[,scsi-id[,reconnect]]]
Adaptec 1542                 aha1542=iobase[,buson,busoff[,dmaspeed]]
Adaptec 274x, 284x           aic7xxx=no_reset (включено если не
ноль)
BusLogic SCSI хосты          buslogic=iobase
Некоторые DELL машины        aic7xxx=no_probe
Future Domain TMC-8xx/950    tmc8xx=mem_base,irq
NCR-основанный SCSI контроллер
                             sym53c8xx=safe:y
Pro Audio Spectrum           pas16=iobase,irq
PS/2 ESDI (MCA)              ed=cyl,head,sec
Seagate ST-0x                st0x=mem_base,irq
Trantor T128                 t128=mem_base,irq

Например:

  boot: live sym53c8xx=safe

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F8СПЕЦИАЛЬНЫЕ ПАРАМЕТРЫ ЗАГРУЗКИ - СИСТЕМА
УСТАНОВКИВы можете использовать следующие параметры загрузки в boot: строка
приглашения, в сочетании с методом загрузки (см. <F3F3>). Эти параметры
управляют работой системы bootstrap.

РЕЗУЛЬТАТ                               ПАРАМЕТР

Отладка с выводом сообщений             DEBCONF_DEBUG=5
Отладка загрузочной последовательности  BOOT_DEBUG=2|3
Отключить framebuffer                   debian-installer/framebuffer=false
Не проверять USB                        debian-installer/probe/usb=false
Не включать PCMCIA                      hw-detect/start_pcmcia=false
Принудительно использовать статическую конфигурацию сети
                                        netcfg/disable_dhcp=true
Задать раскладку клавиатуры             bootkbd=es
Выключить ACPI для PCI карт (полезно для некоторых серверов HP и машин от
VIA)                                    pci=noacpi

Например:

  boot: live debian-installer/framebuffer=false

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F9ПОЛУЧЕНИЕ ПОМОЩИЕсли вы не можете запустить Ubuntu, не отчаивайтесь! Команда Ubuntu готова
помочь вам! Мы особо заинтересованы в выслушивании проблем связанных с
запуском, потому что в основном они не случаются только с одним человеком.
Если мы уже знаем о вашей проблеме, то сможем сделать быстрое исправление,
или мы выслушаем ее и преодолеем ее вместе с вами, и ваш опыт будет полезен
пользователю столкнувшемуся со схожей проблемой!

Прочтите FAQ для для получения дополнительной информации; этот документ
доступен на сайте Ubuntu, http://www.ubuntu.com/

Нажмите F1 для контекстной справки, или Escape для выхода из нее.F10АВТОРСКИЕ ПРАВА И ГАРАНТИИUbuntu охраняется авторским правом (C) 2004-2006 Canonical Ltd., и
объединяет труд многих других авторов и участников.

Система Ubuntu является свободно распространяемой. После запуска, точные
соглашения распространения для каждого пакета описаны в соответствующем
файле\r /usr/share/doc/packagename/copyright.

Ubuntu распространяется АБСОЛЮТНО БЕЗ ГАРАНТИИ,\r на протяжении срока
предусмотренного соответствующим законом.

Эта система основана на Debian. Смотрите на http://www.debian.org/ для
получения деталей и информации о проекте Debian.

Нажмите F1 для контекстной справки, или Escape для выхода из нее. 
